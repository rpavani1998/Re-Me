# The API loads backend/.env when started from backend/.
# Copy backend/.env.example to backend/.env and edit that file.
# See docs/CREDENTIALS.md for provider-specific values and Google Cloud setup.
# The dashboard has its own web/.env.example; never put provider keys in NEXT_PUBLIC_*.
